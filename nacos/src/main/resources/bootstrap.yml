spring:
  application:
    name: nacos-provider
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848
        file-extension: yml
        namespace: ${spring.profiles.active}
        shared-dataids: ${spring.application.name}